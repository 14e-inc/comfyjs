steps:
  - command: echo "I will stick!"
    agents:
      hostname: "$BUILDKITE_AGENT_META_DATA_HOSTNAME"
  - command: |
      ls -la
      cd packages
      ls -la
      cd comfy-auth-util
      npm ci
      npm build